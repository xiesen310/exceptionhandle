/**
  * Created by Allen on 2018/1/30.
  */
object App {

  def main(args: Array[String]): Unit = {
    /* val time = "20180130-152448"
     val timeSplit = time.split("-")
     val timeSection = (timeSplit(1).charAt(0).toString + timeSplit(1).charAt(1).toString).toInt
     println(timeSection)*/

/*    val arr = Array(1, 2, 3, 3, 3)
    val array = arr.distinct
    val format = array.mkString(",")

    val list = List(1, 2, 3, 3, 3)
    val map = Map("a" -> 1, "b" -> 2, "a" -> 2)
    map.keys.foreach(x => {
      println("key:" + x + " , value:" + map.get(x))
    })
    println("map集合： " + map.size)
    println("list集合： " + list.size)
    println("数组: " + arr.length)
    println("数组去重： " + array.length)
    println("数组中的值： "+array)
    println("format数组： "  + format)*/


    /*val str = "12345"
    println(str.substring(2))*/



  }
}
